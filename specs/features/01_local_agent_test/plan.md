# Feature Plan: Fibonacci Python Implementation

## Requirements
- Create a python file named `fib.py` in the root of the workspace.
- Implement a function `fibonacci(n)` that returns the n-th Fibonacci number.
- Handle edge cases where n <= 0.

## Test Criteria
- Must pass standard test definitions via pytest.
